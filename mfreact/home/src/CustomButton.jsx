import React from "react";

const CustomButton = () => {
  return (
    <button onClick={() => console.log("lalalaala")}>
      This Is CustomButton
    </button>
  );
};

export default CustomButton;
